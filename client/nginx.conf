server {
    server_name teamfrankly.kr

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}

# server {
#     listen 80;
#     return 308 https://$host$request_uri;
#     }
# server {
#     listen 443;
#
#     location / {
#         root /usr/share/nginx/html;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
#     }
#
# }
